requests==2.32.5
tzdata==2025.2

# Прямая сверка с Яндекс.Музыкой (yandex_direct.py).
# Модуль опционален: без этих пакетов monitor.py работает по старой цепочке
# iTunes -> song.link -> Musicfetch, но теряет 6 треков из 10 на живом пуле.
yandex-music==2.2.0
thefuzz==0.22.1
python-Levenshtein==0.27.3
